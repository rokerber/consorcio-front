apiVersion: v1
kind: Namespace
metadata:
  name: staging
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consorcio-front-deployment
  namespace: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consorcio-front
  template:
    metadata:
      labels:
        app: consorcio-front
    spec:
      containers:
        - name: consorcio-front-container
          image: k3rb3rdock/consorcio-front:initial # Uma imagem placeholder inicial
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: consorcio-front-service
  namespace: staging
spec:
  selector:
    app: consorcio-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30084 # A porta que abriremos para o mundo exterior
  type: NodePort
